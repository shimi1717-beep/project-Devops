apiVersion: v1
kind: Service
metadata:
  name: {{ include "latest.fullname" . }}
  labels:
    {{- include "latest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "latest.selectorLabels" . | nindent 4 }}
